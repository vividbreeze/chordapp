{
  "name": "chordapp",
  "version": "1.0.0",
  "description": "MIDI Note and Chord Recognizer Web App",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "dev": "npx http-server -c-1"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "http-server": "^14.1.1"
  }
}
